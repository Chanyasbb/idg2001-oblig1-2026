NOTES: 
- browser må fikses 

sander:
- husk å last ned docker på vs code og pc 
- last ned REST client på vs code så du kan runne apis 


Nå:
- bare draft av oppgaven.

1. Start the server: uvicorn app.main:app --reload (from olympics_api/)                                       
  2. Run Create a user first — copy the user_id from the response                                               
  3. Replace every PASTE_USER_ID_HERE with your actual ID                                                       
  4. Run the rest in order      

   cd ~/idg2001-oblig1/olympics_api
  source ../venv/bin/activate                                                                                   
  uvicorn app.main:app --reload